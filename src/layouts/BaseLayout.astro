---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  /** Structured data objects (schema.org) injected as JSON-LD — the AIO backbone. */
  jsonLd?: Record<string, any>[];
  ogType?: string;
  /** Content language. Drives <html lang> + og:locale. 'en' | 'bm' (Malay) | 'zh' (Chinese). */
  lang?: 'en' | 'bm' | 'zh';
}

const { title, description, jsonLd = [], ogType = 'website', lang = 'en' } = Astro.props;
const langCode = { en: 'en-MY', bm: 'ms-MY', zh: 'zh-MY' }[lang];
const ogLocale = { en: 'en_MY', bm: 'ms_MY', zh: 'zh_MY' }[lang];
const canonical = new URL(Astro.url.pathname, Astro.site).href;
const fullTitle = title === 'TheBeautyGPT'
  ? 'TheBeautyGPT — Smarter answers for your skin'
  : `${title} | TheBeautyGPT`;
---
<!doctype html>
<html lang={langCode}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <!-- Open Graph / social -->
    <meta property="og:type" content={ogType} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content="TheBeautyGPT" />
    <meta property="og:locale" content={ogLocale} />
    <meta name="twitter:card" content="summary_large_image" />

    <!-- Organization structured data — establishes the publisher entity for AI engines -->
    <script type="application/ld+json" set:html={JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'Organization',
      name: 'TheBeautyGPT',
      url: 'https://thebeautygpt.com',
      description: 'Evidence-led skincare guidance for Malaysian skin.',
    })} />

    {jsonLd.map((obj) => (
      <script type="application/ld+json" set:html={JSON.stringify(obj)} />
    ))}

    <link rel="sitemap" href="/sitemap-index.xml" />
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
